<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The ONLY permission. Network is used solely to reach Open-Meteo, and only after the user
         opts in to weather enrichment (or actively types a place-name search). See
         docs/PRIVACY.md. Deliberately NO location permissions: the app never reads device
         location — the home location is typed by the user (searched, GPS pair, or Plus Code). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Megrim"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package-visibility (Android 11+/API 30+): without this, url_launcher's canLaunchUrl()
             returns false for https URLs, so the Donate and Source-code tiles silently no-op.
             Declares that we resolve a browser to open https links. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
